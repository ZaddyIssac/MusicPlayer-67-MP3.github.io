/* DIVs 2D Rectangles
*/
//
println(displayWidth, displayHeight);
fullscreen();
int appWidth = displayWidth;
int appHeight = displayHeight;
//
int paperWidth = 14.2;
int paperHeight = 28;

//rect ( DivX, DivY, DivWidth, DivHeight ) ;
//
float SongTitleFullBoxX = appWidth * 3 / paperWidth ;
float SongTitleFullBoxY = appHeight * 0.5 / paperHeight ;
float SongTitleFullBoxWidth = appWidth * 8.4 / paperWidth ;
float SongTitleFullBoxHeight = appHeight * 2 / paperHeight ;
//
float SongTitleX = appWidth * 5.7 / paperWidth ;
float SongTitleY = appHeight * 0.5 / paperHeight ;
float SongTitleWidth = appWidth * 5 / paperWidth ;
float SongTitleHeight = appHeight * 2 / paperHeight ;
//
float ArtistNameX = appWidth * 5.7/ paperWidth ;
float ArtistNameY = appHeight * 2.3 / paperHeight ;
float ArtistNameWidth = appWidth * 5 / paperWidth ;
float ArtistNameHeight = appHeight * 0.7 / paperHeight ;
//
float XButtonX = appWidth * 13.1 / paperWidth ;
float XButtonY = appHeight * 0 / paperHeight ;
float XButtonWidth = appWidth * 1 / paperWidth ;
float XButtonHeight = appHeight * 1.4 / paperHeight ;
//
float PlayBackX = appWidth * 1.3 / paperWidth ;
float PlayBackY = appHeight * 3 / paperHeight ;
float PlayBackWidth = appWidth * 12 / paperWidth ;
float PlayBackHeight = appHeight * 1.3 / paperHeight ;
//
float PlayBackBarX = appWidth * 1.6 / paperWidth ;
float PlayBackBarY = appHeight * 3.9 / paperHeight ;
float PlayBackBarWidth = appWidth * 11.5 / paperWidth ;
float PlayBackBarHeight = appHeight * 0.3 / paperHeight ;
//
float LyricsX = appWidth * 5.2 / paperWidth ;
float LyricsY = appHeight * 4.6 / paperHeight ;
float LyricsWidth = appWidth * 8.3 / paperWidth ;
float LyricsHeight = appHeight * 7.1 / paperHeight ;
//
float ImageX = appWidth * 1.3 / paperWidth ;
float ImageY = appHeight * 15.5 / paperHeight ;
float ImageWidth = appWidth * 11.4 / paperWidth ;
float ImageHeight = appHeight * 11.4 / paperHeight ;
//
float PlayX = appWidth * 1.3 / paperWidth ;
float PlayY = appHeight * 4.6 / paperHeight ;
float PlayWidth = appWidth * 1 / paperWidth ;
float PlayHeight = appHeight * 1 / paperHeight ;
//
float PauseX = appWidth * 2.3 / paperWidth ;
float PauseY = appHeight * 4.6 / paperHeight ;
float PauseWidth = appWidth * 1 / paperWidth ;
float PauseHeight = appHeight * 1 / paperHeight ;
//
float FFX = appWidth * 1.3 / paperWidth ;
float FFY = appHeight * 5.6 / paperHeight ;
float FFWidth = appWidth * 1 / paperWidth ;
float FFHeight = appHeight * 1 / paperHeight ;
//
float FRX = appWidth * 2.3 / paperWidth ;
float FRY = appHeight * 5.6 / paperHeight ;
float FRWidth = appWidth * 1 / paperWidth ;
float FRHeight = appHeight * 1 / paperHeight ;
//
float FavouriteX = appWidth * 1.3 / paperWidth ;
float FavouriteY = appHeight * 7.6 / paperHeight ;
float FavouriteWidth = appWidth * 1 / paperWidth ;
float FavouriteHeight = appHeight * 1 / paperHeight ;
//
float ShuffleX = appWidth * 1.3 / paperWidth ;
float ShuffleY = appHeight * 6.6 / paperHeight ;
float ShuffleWidth = appWidth * 1 / paperWidth ;
float SHuffleHeight = appHeight * 1 / paperHeight ;
//
float LoopX = appWidth * 2.3 / paperWidth ;
float LoopY = appHeight * 6.6 / paperHeight ;
float LoopWidth = appWidth * 1 / paperWidth ;
float LoopHeight = appHeight * 1 / paperHeight ;
//
float BookMarkX = appWidth * 2.3 / paperWidth ;
float BookMarkY = appHeight * 7.6 / paperHeight ;
float BookMarkWidth = appWidth * 1 / paperWidth ;
float BookMarkHeight = appHeight * 1 / paperHeight ;
